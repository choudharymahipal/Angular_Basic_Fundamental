import { AttrHighlightDirective } from './attr-highlight.directive';

describe('AttrHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new AttrHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
