import { CustomeColorDirective } from './custome-color.directive';

describe('CustomeColorDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomeColorDirective();
    expect(directive).toBeTruthy();
  });
});
