import { CustomeWordCountPipe } from './custome-word-count.pipe';

describe('CustomeWordCountPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomeWordCountPipe();
    expect(pipe).toBeTruthy();
  });
});
